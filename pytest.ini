[pytest]
# Lets `python -m pytest python-shim/tests static-planner/tests benchmarks/tests`
# run from the repository root (see AGENTS.md) without editable installs.
pythonpath =
    python-shim/src
    static-planner/src
    .

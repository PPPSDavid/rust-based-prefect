# Archived perf dumps

Historical `perf_matrix.py` candidate and compare artifacts. They are **not** published on the MkDocs site (`exclude_docs: archive/**`).

Canonical live artifacts stay at the repo root of `docs/`:

- `docs/perf_matrix_results.json` / `docs/perf_matrix_summary.md` — latest `perf_matrix.py run`
- `docs/perf_comparison.json` — Prefect vs IronFlow A/B (`compare_prefect_vs_ironflow.py`)
- `docs/perf_methodology.md` — harness semantics (in-repo only)
